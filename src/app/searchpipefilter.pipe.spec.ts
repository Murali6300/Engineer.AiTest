import { SearchpipefilterPipe } from './searchpipefilter.pipe';

describe('SearchpipefilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchpipefilterPipe();
    expect(pipe).toBeTruthy();
  });
});
